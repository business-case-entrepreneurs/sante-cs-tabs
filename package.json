{
  "name": "sante-cs-tabs",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && rollup -c && web-ext build --config config/web-ext.config.js",
    "clean": "rimraf build dist",
    "serve": "concurrently -k -n \"rollup,webext\" \"rollup -cw\" \"npm run serve:web-ext\"",
    "serve:web-ext": "web-ext run --config config/web-ext.config.js -u http://localhost:3000/",
    "sign": "web-ext sign --config config/web-ext.config.js --api-key=$AMO_JWT_ISSUER --api-secret=$AMO_JWT_SECRET"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "rimraf": "^3.0.0",
    "rollup": "^1.27.5",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-json": "^4.0.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-typescript2": "^0.25.2",
    "typescript": "^3.7.2",
    "web-ext": "^3.2.1",
    "web-ext-types": "^3.2.1",
    "webextension-polyfill": "^0.5.0"
  }
}
